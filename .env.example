# Comma-separated GitHub usernames displayed on the Members page.
# Copy this file to .env.local for local development (that file is gitignored).
# In CI, add GITHUB_MEMBERS as a repository secret in GitHub → Settings → Secrets and variables → Actions.
GITHUB_MEMBERS=handle1,handle2,handle3
